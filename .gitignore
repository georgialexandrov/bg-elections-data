# Database — distributed via GitHub Releases (too large for git)
elections.db
elections.db-shm
elections.db-wal
elections_debug.db

# Internal build tooling, raw data, and planning docs
.internal/

# CIK data exports (zips + extracted dirs) — available from GitHub Releases
data/cik-exports/

# Obsolete scripts (kept in parent hackathon dir)
data/parse.py
data/geocode_locations.py

# Tool configs
.claude/
.codex/
.mcp.json

# Python
__pycache__/
*.pyc

# Node
node_modules/
dist/

# OS
.DS_Store

# Temp parser DBs
/tmp/elections_*.db

# OG image cache (generated on demand)
og-cache/

# Playwright screenshots and reports (ephemeral — run `pnpm screenshots` to regenerate)
web/screenshots/
web/test-results/
web/playwright-report/
